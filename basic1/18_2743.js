// const fs = require("fs");
// const input = fs.readFileSync("/dev/stdin").toString().trim().split("\n");
let input = `pulljima`.split('\n');


const sol = (input) => {
	return input[0].length;
}

console.log(sol(input));
